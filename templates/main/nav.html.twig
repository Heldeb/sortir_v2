<div class="navbar-container">
    <nav class="navbar">
        <div class="navbar-brand">
            <img src="{{ asset('img/logo/logo.png') }}" alt="Notre logo !">
        </div>

        <ul>

            {# visible en toute circonstances #}

            <li><a href="{{ path('main_home') }}" title="Accueil">Accueil</a></li>
            {# <a href="{{ path('main_home') }}" title="Accueil">Accueil</a></li> #}
            <li><a href="{{ path('vue_sorties') }}" title="Liste des activités">Sorties</a></li>
            <li><a href="{{ path('main_presentation') }}" title="Présentation">Présentation</a></li>

            {# visible après enregistrement #}

            {% if app.user %}
                <li><a href="{{ path('app_profil') }}" title="Mon profil">Mon profil</a> </li>
                <li><a href="{{ path('app_logout') }}" title="Se déconnecter">Se déconnecter</a> </li>

                {# visible par l'administrateur #}

                {% if is_granted("ROLE_ADMIN") %}
                <li><a href="{{ path('gestion_villes') }}" title="Villes">Villes</a></li>
                <li><a href="{{ path('gestion_sites') }}" title="Sites">Sites</a></li>

            {% endif %}

            {% else %}
                <li><a href="{{ path('app_login') }}" title="Login">Login</a></li>
                <li><a href="{{ path('app_registration') }}" title="Create account">Register</a></li>
            {% endif %}
        </ul>
    </nav>
</div>